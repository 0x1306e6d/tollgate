tollgate {
  routing {
    getContestType {
      method = "GET"
      path = "/api/v2/contest-type/{idOrName}"
      upstream = ${tollgate.upstreams.contest}
    }
  }
  upstreams {
    contest {
      uri = "http://contest:8080"
    }
  }
}
